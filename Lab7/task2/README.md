task 2 - OOP in python
Files:
`models.py` - base class + child classes
`main.py` - demo script
Structure:
`Animal` is the base class with `name`, `age`, `weight` attributes and a `speak()` method.

`Dog` and `Cat` inherit from it. Each adds its own attribute and overrides `speak()`.